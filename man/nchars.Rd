% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nchars.R
\name{nchars}
\alias{nchars}
\alias{nchars,partition-method}
\alias{nchars,subcorpus-method}
\alias{nchars,partition_bundle-method}
\alias{nchars,subcorpus_bundle-method}
\alias{nchars,corpus-method}
\title{Count the number of characters}
\usage{
nchars(x, ...)

\S4method{nchars}{partition}(
  x,
  p_attribute = "word",
  char_regex = "[a-zA-Z]",
  lowercase = TRUE,
  decreasing = TRUE
)

\S4method{nchars}{subcorpus}(
  x,
  p_attribute = "word",
  char_regex = "[a-zA-Z]",
  lowercase = TRUE,
  decreasing = TRUE
)

\S4method{nchars}{partition_bundle}(x, mc = FALSE, progress = TRUE, decreasing = TRUE, ...)

\S4method{nchars}{subcorpus_bundle}(x, decreasing = TRUE, mc = FALSE, progress = TRUE, ...)

\S4method{nchars}{corpus}(
  x,
  p_attribute = "word",
  lowercase = TRUE,
  sample = 5000000L,
  char_regex = "[a-zA-Z]",
  decreasing = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{x}{Object to process.}

\item{...}{Argument passed into \code{blapply()}.}

\item{p_attribute}{the p-attribute}

\item{char_regex}{if NULL, counts for all charactrs will be returned, else a regex indicating which characters to include in the counting}

\item{lowercase}{whether to lower tokens}

\item{decreasing}{logical, passed into order call}

\item{mc}{logical}

\item{progress}{A \code{logical} value.}

\item{sample}{An \code{integer} or \code{numeric} value defining the number of sample
tokens extracted from the (entirely decoded) token stream to be evaluated.}

\item{verbose}{Whether to output progress messages.}
}
\description{
Count the number of characters
}
\examples{
library(polmineR)
use("RcppCWB")

partition("REUTERS", id = "127") \%>\%
  nchars()
  
corpus("REUTERS") \%>\%
  subset(id == "127") \%>\%
  nchars()
  
corpus("REUTERS") \%>\%
  partition_bundle(s_attribute = "id") \%>\%
  nchars()
  
corpus("REUTERS") \%>\%
  split(s_attribute = "id") \%>\%
  nchars()
library(polmineR)
use("RcppCWB")
n <- corpus("REUTERS") \%>\% nchars(sample = 4000)
}
